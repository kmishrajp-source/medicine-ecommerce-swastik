import DownloadsClient from './DownloadsClient';

export const metadata = {
  title: 'Download Center | Swastik Medicare',
  description: 'Download corporate profiles, media kits, ISO certificates, and investor prospectuses from Swastik Medicare.',
};

export default function DownloadsPage() {
  return <DownloadsClient />;
}
