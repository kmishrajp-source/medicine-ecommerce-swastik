import MediaClient from './MediaClient';

export const metadata = {
  title: 'News & Media | Swastik Medicare',
  description: 'Latest news, press releases, events, blogs, and media resources from Swastik Medicare — India\'s AI-powered digital healthcare platform.',
};

export default function MediaPage() {
  return <MediaClient />;
}
