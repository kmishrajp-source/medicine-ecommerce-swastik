import VerificationClient from './VerificationClient';

export const metadata = {
  title: 'Provider Verification | Admin',
};

export default function ProviderVerificationAdminPage() {
  return <VerificationClient />;
}
