import SecurityClient from './SecurityClient';

export const metadata = {
  title: 'Security & Audit Framework | Admin',
};

export default function SecurityAdminPage() {
  return <SecurityClient />;
}
